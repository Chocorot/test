const menuToggle = document.querySelector('.burger');
const navlinks = document.querySelector('.navlinks');

menuToggle.addEventListener('click', function() {
  menu.classList.toggle('show-navlinks');
});